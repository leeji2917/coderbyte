def Palindrome(str)

  result = true
  if str != str.reverse
    result=false
  end
   return result

end
   
# keep this function call here 
# to see how to enter arguments in Ruby scroll down   
Palindrome(STDIN.gets)           
